@code {
    public const string Id = "websocket";
    private string _internalId { get; set; } = Guid.NewGuid().ToString();

    [Parameter]
    public string Role { get; set; } = OdysseyRoles.None;

}
<template id="websocket" hx-ext="ws" ws-connect="wss:/ws/@Role"></template>