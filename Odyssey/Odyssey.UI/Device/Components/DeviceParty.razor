@using Haondt.Web.UI.Components.Containers
@using Odyssey.UI.Core.Services
@attribute [RenderPage(Role = OdysseyRoles.Device)]
@attribute [LayoutOptions(FillPage = true, UseBottomSheet = true)]
@implements IHasNavigationBarComponent
@code {
    public NavigationBar CreateNavigationBar() => new() { Template = new NavigationBarEmpty() };
}
<ContentContainer AddClasses='"big-screen"' Layout="ContentContainerLayout.Center">
    <Frame Flex="FrameDirection.Col" Align="FrameAlignment.Center">
        <h1 id="hero-text" class="fg-primary uppercase font-semibold idle-brand-display-text">Odyssey</h1>
        <p>Waiting for host to start a session</p>
    </Frame>
</ContentContainer>
